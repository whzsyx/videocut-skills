# 时间线与导出

- 时间线消费剪后时间、`source_cut.mp4`、规范 `subtitles.srt` 和已审核动画模块。
- 场景连续、无重叠、无空洞；HTML cue 必须引用已审核模块。
- 字幕位于安全区，不遮挡主视觉；没有字幕产物时不得生成字幕层。
- 预览与导出使用相同的固定逻辑画布，避免画面大小在成片中变化。
- 导出后验证视频流、音频流、分辨率、帧率、总时长与关键帧。
- 抽检所有 HTML 模块，不只检查刚修改的一页。
- 发现黑边、裁切、遮挡、错误素材、静音或时长漂移时，修候选/Runtime 后重新导出。
