# 动画规则

- 动画必须绑定具体口播句、cue 和逐步动作；禁止只有 `step 1/2/3`。
- 一帧只保留一个主视觉来源，可叠加短标签、箭头、圈选和局部高亮。
- 操作画面、产品结果与证明页面优先真实截图或录屏，不默认重画。
- 没有必要动画时提交 `modules: []` 与非空 `noAnimationReason`。
- 生成候选后逐模块检查第一步、最后一步和时间线中的实际时间点。
- 画面不得出现内部文件名、素材路径、工作台说明或实现提示。
